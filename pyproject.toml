[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.mypy]
files = "src"
python_version = "3.7"

[tool.black]
line-length = 100
target-version = ["py310", "py39", "py38", "py37"]
